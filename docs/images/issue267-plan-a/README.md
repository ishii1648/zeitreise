# #267 案A 実装の確認画像（AC12）

1000 / 1100 / 1300 年の西欧を 1412×1114 相当の viewport で z4 / z5 / z7
それぞれ撮影したもの（PR #267 実装ブランチの検証エビデンス。縮小 JPEG）。

再生成: dev サーバ起動後、ヘッドレス CDP ハーネス
（`deno run -A scripts/verify/cdp.ts <URL> <harness>`）で
`?year=<Y>&zoom=<Z>&center=<西欧>` を 1412×1114 で撮影する。
